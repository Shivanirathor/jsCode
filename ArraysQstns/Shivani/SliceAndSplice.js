var users = ["A", "B", "C", "D", "E"];

console.log(users.slice(1,3));   // slice means - jo hum no pass krege uska nya array bnakr d dega like isme 1 se 3 s less tk ke elements prnt honge

users.splice(1,3,"hlo");     // splice means- 1 value is for stating and 2 is for count and later we hv to put text
console.log(users);